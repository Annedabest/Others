# API Service

This NestJS service exposes backend endpoints for the Finance & Receipt Platform.

## Current Endpoints
- `GET /api/health`: health check returning service status and environment information.

All other routes will return 404 until features are implemented.
